import Nimble
@testable import PlaylistFeature
import Quick
import ReactorKit
import XCTest

final class MyPlaylistDetailSpec: QuickSpec {
    override class func spec() {
      

        describe("MyPlaylistDetailReactorTest") {
//            context("viewDidLoad") {
//                reactor.action.onNext(.viewDidLoad)
//
//                let state = reactor.currentState
//
//                // expect(state.dataSource.songs.count).to(equal(9))
//
//                print(state.dataSource.songs.map{$0.title})
//                reactor.action.onNext(.itemDidMoved(0, 8))
//
//                print(state.dataSource.songs.map{$0.title})
//            }
//
//
        }
    }
}
